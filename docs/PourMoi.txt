Commandes de debug console (F12) :
  window.store.addBits(1000000)       — ajouter 1M bits
  window.store.getState()             — voir l'état complet
  window.store.resetState()           — reset la partie
